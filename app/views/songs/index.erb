<% @songs.each do |song| %>
  <div class="song">
    <h3><%= song.title %> <small>(by: <%= song.author %>)</small></h3>
    <p>
      <%= song.url %>
    </p>
  </div>
<% end %>


<a href="/messages/new">Add a song... It's FREE (for now)!</a>

